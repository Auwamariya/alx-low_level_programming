#include <stdio.h>

/**
 * main - display name of program
 * @argc: Number of command line arguments
 * @argv: Array name
 * Return:int
 */

int main(int argc, char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
